(defsrc    
    caps
    spc
    tab
    ret
    k
    j
    h
    l
)

(defalias
    Fsuper (tap-hold 1 150 f lmeta)  
    spaceAlt (tap-hold 1 200 spc lalt)
    escCtrl (tap-hold 1 150 esc lctrl)  
    tab_arrows(tap-hold 1 150 tab (layer-toggle arrows) ) 
    return_ctrl(tap-hold 1 150 ret rctrl)
)

(deflayer base
    @escCtrl
    @spaceAlt
    @tab_arrows
    @return_ctrl
    k
    j
    h
    l
)

(deflayer arrows
  _
  _
  _
  _
  up
  down
  left
  right
) 
